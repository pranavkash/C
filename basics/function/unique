#include <stdio.h>
int printunique(int *arr,int size){

    for(int i=0;i<size;i++){
        for(int j=i;j<size;j++){
            if(arr[i]=arr[j])//if the element exsists in fowarted path dont print or else print.this 
            //another way is back checking. if available on backcking then dont print.
        }
    }

}

int main () {
    int size;
    printf("enter the size of arr");
    scanf("%d",&size);
    int arr[size];
    for(int i=0;i<size;i++){
        for(int j=0;)
        scanf("%d",&arr[i]);
    }

    printunique(arr,size);
}